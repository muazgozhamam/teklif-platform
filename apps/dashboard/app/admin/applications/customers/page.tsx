'use client';

import { ApplicationsListPage } from '../_components/applications-shared';

export default function Page() {
  return <ApplicationsListPage title="Müşteri Adayları" subtitle="Alıcı / kiracı talepleri." forcedType="CUSTOMER_LEAD" />;
}
